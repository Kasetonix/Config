sudo - wykonanie komendy jako użytkownik root (Administartor)
ls - pokazanie zawartości folderu
la - pokazanie zawartości folderu razem z plikami ukrytymi
cd - przemieszczenie się do folderu o podanej ścieżce
pwd - wypisanie ścieżki folderu, w którym się znajdujemy
file - podaje typ pliku o podanej ścieżce
clear - czyści ekran
touch - tworzy plik o danej nazwie
mv - przenosi bądź zmienia nazwy plików
cp - kopiuje pliki
mkdir - tworzenie folderu o danej ścieżce
pushd - działa jak cd, ale dodatkowo podaję ścieżkę folderu, do którego przeszliśmy i ścieżkę folderu z którego przeszliśmy
popd - wypisanie ścieżki poprzedniego folderu, w którym się znajdowaliśmy
locate - zlokalizowanie pliku o danej nazwie we wszystkich katalogach
updatedb - aktualizuje systemowe bazy danych
which - sprawdza i lokalizuje obecność komend
date - podaje datę
cal - podaje kalendarz
history - wypisuje ostatnie komendy (dużo!)
whatis - wypisuje do czego służy dana komenda
man - włącza instrukcję obsługidanego pakietu
apropos - wypisuje komendy których można użyć do zrobienia czegoś

